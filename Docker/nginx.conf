# Configuration 1: Round Robin (par défaut)
# upstream store_manager_rr {
#     server store_manager_1:8080;
#     server store_manager_2:8080;
#     server store_manager_3:8080;
# }

# Configuration 2: Least Connections (votre config actuelle)
upstream store_manager_lc {
    least_conn;
    server store_manager_1:8080;
    server store_manager_2:8080;
    server store_manager_3:8080;
}

# Configuration 3: IP Hash
# upstream store_manager_hash {
#     ip_hash;
#     server store_manager_1:8080;
#     server store_manager_2:8080;
#     server store_manager_3:8080;
# }

# Configuration 4: Weighted Round Robin
# upstream store_manager_weighted {
#     server store_manager_1:8080 weight=3;
#     server store_manager_2:8080 weight=2;
#     server store_manager_3:8080 weight=1;
# }

server {
    listen 80;
    
    location / {
        proxy_pass http://store_manager;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
        proxy_buffering off;
    }

    # Endpoint de health check pour toutes les instances
    location /health {
        proxy_pass http://store_manager;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Métriques Prometheus depuis store_manager_1 seulement
    location /metrics {
        proxy_pass http://store_manager_1:8080/metrics;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}